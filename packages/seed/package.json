{
  "name": "@workflow/seed",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "시드 콘텐츠 팩 — 도구 카탈로그 · 부서 칩 · 예외 프롬프트 · 시드 업무 흐름 14개. 산문이 아니라 타입 검사되는 데이터.",
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --test \"test/*.test.ts\"",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@workflow/graph-core": "*",
    "typescript": "^5.8.0"
  },
  "//dependencies": "★ 비어 있어야 한다. 시드는 데이터다. graph-core는 타입 검증과 테스트에만 쓰이므로 devDependency이고, src의 import는 전부 `import type`이라 런타임에 사라진다."
}
