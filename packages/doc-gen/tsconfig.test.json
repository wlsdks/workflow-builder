{
  // 테스트만 Node 전역을 본다. src는 types:[] 아래에서 컴파일되므로
  // 소스에서 실수로 node:fs를 쓰면 컴파일이 깨진다.
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "types": ["node"]
  },
  "include": ["src/**/*.ts", "test/**/*.ts"]
}
