{
  "name": "@workflow/paste-parse",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "붙여넣기 파서 — 원문 위 스팬 대수만 다루는 무손실 파이프라인. React·DB·DOM 의존 없음 (PARSING.md).",
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --test \"test/*.test.ts\"",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json",
    "lint": "eslint src test"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.8.0"
  },
  "//dependencies": "★ 런타임 의존성은 영원히 비어 있어야 한다 (graph-core와 같은 계약, D-119). 워커에도 그대로 올라가야 하므로 무언가를 넣고 싶어지면 그것은 이 패키지에 들어갈 코드가 아니다."
}
