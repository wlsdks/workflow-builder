/**
 * packages/paste-parse/src/lexicon/flow.ts  (PARSING §4.4)
 *
 * 인계 · 종결 · 예외 신호.
 *
 * `EXCEPTION.opener`로 시작하는 줄은 **단계로 만들지 않고 직전 항목의 예외 각주로 붙인다.**
 * PRD가 "예외는 존재가 아니라 빈도로 묻는다"고 한 그 예외 슬롯이다.
 * 별도 단계로 만들면 흐름이 예외로 오염된다.
 */

export const HANDOFF = {
  verb: /(?:넘기|넘겨|전달|이관|인계|배정|할당|넘어가|패스|포워딩|포워드|토스)/,
  dative: /([가-힣]{1,10}(?:팀|부서|파트|실|본부|센터|과|님|씨|담당자|담당|사|측))(?:에게|한테|께|으로|로|에)/,
  polite: /(?:요청\s?드리|부탁\s?드리|공유\s?드리|전달\s?드리|말씀\s?드리|보내\s?드리)/,
} as const;

export const TERMINAL = {
  word: /(?:완료|마무리|마감|종료|끝(?:이에요|입니다|납니다|)|종결|클로즈|closing|보관|아카이브|파일링|철하|스캔해서\s?보관)/,
  phrase: /(?:여기까지|이걸로\s?끝|이후\s?없|더\s?이상\s?없|그러면\s?끝)/,
} as const;

export const EXCEPTION = {
  opener: /(?:^|\s)(?:단,|다만,?|예외적으로|특이사항|주의|참고로|유의|except|간혹|가끔|드물게|보통은\s?아니지만)/,
  negativeCase:
    /(?:안\s?될\s?경우|안\s?되면|불가(?:능)?할\s?때|문제(?:가)?\s?생기면|오류(?:가)?\s?나면|누락(?:되면|된\s?경우)|틀리면|실패(?:하면|시))/,
} as const;
