# @workflow/paste-parse

> 붙여넣은 글 → 단계 후보

붙여넣기 파서 — 원문 위 스팬 대수만 다루는 무손실 파이프라인. React·DB·DOM 의존 없음 (PARSING.md).

| | |
|---|---|
| 명세 | [`docs/PARSING.md`](../../docs/PARSING.md) |
| 런타임 의존성 | **없음** |
| 소스 | 3543줄 |
| 테스트 | 59건 |

```bash
npm test          # 골든 픽스처 + 불변식
npm run typecheck # 순수성이 타입으로 증명된다
```

이 패키지의 규칙과 그 근거는 명세 문서에 있다. 코드에서 명세와 갈라진 자리는
전부 주석으로 이유를 남겼고, 테스트가 그 판단을 고정한다.
